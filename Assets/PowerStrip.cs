using UnityEngine;
using System.Collections;

public class PowerStrip : MonoBehaviour {

	// Use this for initialization
	void Start () {
		GetComponent<ColorManager>().RandomizeColor();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
